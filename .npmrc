# Поднимаем транзитивные зависимости mermaid в корень node_modules,
# иначе Vite optimizeDeps (withMermaid) не резолвит их при строгой структуре pnpm.
public-hoist-pattern[]=*cytoscape*
public-hoist-pattern[]=dayjs
public-hoist-pattern[]=@braintree/sanitize-url