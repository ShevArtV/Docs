# Статические секции

Статическая секция — это секция, одинаковая для всех страниц (шапка, подвал, сквозные блоки). Её контент берётся не из самой страницы, а из ресурса «Типы страниц», поэтому правится в одном месте.

::: info Раздел в разработке
Ниже — план раздела. Содержимое наполняется итеративно.
:::

## Как пометить секцию статической

<!-- атрибут data-mpc-static; поле is_static в конфиге; что меняется при нарезке -->

## Каскад значений и стилей

<!-- откуда берётся контент (Типы страниц), merge-split в Render, каскад настроек/стилей из ресурса -->

## Лексиконы статических секций

<!-- уровень page-types; перевод обычной секции в статическую → чистка ресурсных ключей (OnDocFormSave); связь с разделом «Лексиконы» -->

## Перевод секции из обычной в статическую и обратно

<!-- что происходит с контентом и лексиконами; нужна ли перенарезка -->
