# Нарезка

::: warning Раздел в работе (TODO)
Здесь будет полное описание процесса нарезки:

- **как запустить** — скрипт `mgr_tpl` / CLI, варианты (один шаблон vs `all`), ключи (обновление контента `--upd` и пр.);
- **что происходит при каждом варианте** — чем отличается обычная нарезка от обновления контента, что перезаписывается, а что сохраняется;
- **куда и что складывается** — файлы секций (`sections/`), чанки (`chunks/`), запечённые страницы (`parsed/`), лексиконы; конфиги MIGX (`mpc_config`), TV, настройки;
- **кэш** — что и когда инвалидируется, `mpc_dev_mode`, очистка `parsed/` и кэша компонента;
- **общая картина** — последовательность шагов, которые компонент выполняет при нарезке.
:::

О том, как создаются и обновляются элементы сайта, читайте в разделе [Создание и обновление элементов](elements).
