# Консольные команды (CLI)

Кроме старых скриптов нарезки (`slice_tpl.php`, `mgr_tpl.php`, `mgr_elems.php` — см. [Начало работы](setup) и [Создание и обновление элементов](elements)), у компонента есть CLI-обёртка `console/mpc` для работы с манифестами, настройками и ClientConfig.

::: info Раздел в разработке
Ниже — план раздела. Содержимое наполняется итеративно.
:::

## Обёртка console/mpc

<!-- запуск, php-версия, базовые команды -->

## Манифесты

<!-- mpc_manifests_path (резолв от core/, env MPC_MANIFESTS_PATH > настройка > дефолт); apply без аргумента -->

## Настройки и ClientConfig

<!-- modContextSetting (per-key context), cgSetting/cgContextValue; settings/clientconfig list с фильтрами --namespace/--context/--group/--key -->
